---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zwave-pvc
  namespace: hass
  # labels:
  #   kasten.io/backup-volume: "enabled"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
