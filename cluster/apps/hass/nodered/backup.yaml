---
apiVersion: stash.appscode.com/v1beta1
kind: BackupConfiguration
metadata:
  name: nodered-backup
  namespace: hass
spec:
  repository:
    name: b2-repo
  schedule: "05 22 * * *"
  target:
    ref:
      apiVersion: apps/v1
      kind: Deployment
      name: node-red
    volumeMounts:
      - name: config
        mountPath: /data
    paths:
      - /data
  retentionPolicy:
    name: 'keep-last-7'
    keepLast: 7
    prune: true
