jobLabel: traefik-metrics
selector:
  matchLabels:
    app.kubernetes.io/instance: traefik
    app.kubernetes.io/name: traefik
namespaceSelector:
  matchNames:
    - networking
endpoints:
  - port: metrics
    path: /
    # path: /metrics
